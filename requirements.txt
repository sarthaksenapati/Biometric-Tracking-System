# ── Core Deep Learning ──────────────────────────────────────
torch
torchvision
torchaudio

# ── Computer Vision ─────────────────────────────────────────
ultralytics
opencv-python
opencv-python-headless  # For headless deployment

# ── Data handling ───────────────────────────────────────────
numpy
pandas
scikit-learn

# ── Face Recognition ────────────────────────────────────────
deepface
onnxruntime  # For InsightFace support

# ── Person Re-Identification ──────────────────────────────────────
torchreid

# ── Backend API ─────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0

# ── Dashboard ──────────────────────────────────────────────
streamlit>=1.28.0,<1.50.0  # Pin before 1.50 bug (queue.SimpleQueue)

# ── Database ────────────────────────────────────────────────
psycopg2-binary>=2.9.0  # PostgreSQL adapter (binary avoids build deps)

# ── Caching & Queuing ─────────────────────────────────────
redis>=5.0.0  # Redis client

# ── Message Queue (optional - for RabbitMQ support) ────────
# pika>=1.3.0  # Uncomment if using RabbitMQ

# ── Utilities & Visualization ──────────────────────────────
matplotlib
pillow

# 🔹 Core Deep Learning (GPU-compatible if CUDA torch installed separately)
torch
torchvision

# 🔹 Computer Vision
ultralytics
opencv-python

# 🔹 Data handling
numpy

# 🔹 Face Recognition
deepface
onnxruntime  # For InsightFace support

# 🔹 Backend API
fastapi
uvicorn

# 🔹 Dashboard
streamlit>=1.28.0,<1.50.0  # Pin before 1.50 bug